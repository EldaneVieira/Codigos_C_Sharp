This project illustrate how the framework GMap can be used to set pins in locations of a map, and how a line can be set between two point, indicating the distance among those points.
It was developed using the C# programming language.
